generated: '2026-08-01'
method: searched
source: https://github.com/Conviva/mcp-marketplace/tree/main/claude-plugin/skills
description: >-
  These are Conviva's OWN Agent Skills, published by Conviva in its Claude plugin marketplace and
  saved here VERBATIM — not generated by the API Evangelist pipeline. They are shipped as part of
  the `conviva-dpi-mcp` plugin (v1.1.1) and load alongside the hosted Conviva DPI MCP server at
  https://dpi-mcp.conviva.com/mcp. Every operation each skill names is an MCP tool on that server,
  not a REST operationId — Conviva publishes no OpenAPI to ground REST-side skills in.
upstream:
  repo: https://github.com/Conviva/mcp-marketplace
  plugin: conviva-dpi-mcp
  plugin_version: 1.1.1
  marketplace: conviva
  install: /plugin install conviva-dpi-mcp@conviva
  note: >-
    "This repository is a published mirror. It is generated from Conviva's internal source of truth
    on each release — do not edit it directly."

skills:
- file: conviva-exploring-context-center.md
  name: exploring-context-center
  description: >-
    Finds Conviva Context Center assets by meaning or topic, then hydrates the full structured
    definition — search the knowledge layer first, then fetch the asset.
  api: https://dpi-mcp.conviva.com/mcp
  service: /context-center
  operations:
  - identity-c3-account-list
  - context-center-nexa-semantic-search
  - context-center-nexa-categories-list
  - context-center-nexa-knowledge-get
  - context-center-asset-get
  - context-center-asset-list
  supporting_files:
  - conviva-exploring-context-center-reference.md
- file: conviva-querying-predefined-metrics.md
  name: querying-predefined-metrics
  description: >-
    Runs a predefined Conviva Context Center metric by its id over a date range, optionally broken
    down by dimension ids — using asset ids only, never raw SQL or a raw query payload.
  api: https://dpi-mcp.conviva.com/mcp
  service: /metric-query
  operations:
  - identity-c3-account-list
  - context-center-asset-get
  - context-center-asset-list
  - context-center-nexa-semantic-search
  - metric-query-run
  - nexa-analyze
- file: conviva-retrieving-behavior-segment-details.md
  name: retrieving-behavior-segment-details
  description: >-
    Retrieves the stored details of an Insights behavior segment and interprets its pre-computed
    insights, evidence and conversion barriers — a static lookup of a segment Insights already
    computed offline, not a live analytical query.
  api: https://dpi-mcp.conviva.com/mcp
  service: /insights
  operations:
  - identity-c3-account-list
  - insights-behavior-segment-list
  - insights-behavior-segment-get
  supporting_files:
  - conviva-retrieving-behavior-segment-details-fields.md

x-evidence:
  fetched: '2026-08-01'
  http_status: 200
  files_saved_verbatim: true
